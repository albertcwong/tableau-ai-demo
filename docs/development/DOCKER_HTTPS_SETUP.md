# Docker HTTPS Setup

Docker HTTPS setup is documented in [Docker Development Guide](./DOCKER_DEV.md).

Quick reference:
- Generate certs: `cd shared && ./generate-cert.sh`
- Start stack: `./scripts/dev-docker.sh up`
- Get URLs: `./scripts/dev-docker.sh url`
